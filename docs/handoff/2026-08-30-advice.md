# 代管期建议单（非可写域）— 2026-08-30（Z code）

> §3 纪律：非可写域（两工程 / 全局 skills / <工作区根>\.claude hooks）只出建议，不动代码。
> 以下均 2026-08-30 只读核查，含正面结论（"查了无问题"也是结论）。

## A-01 toolkit `skills/fresh-checker/` 与全局镜像：双份一致，无漂移 ✅

对 `skills/fresh-checker/` 全部文件与 `%USERPROFILE%\.claude\skills\fresh-checker\`
逐文件 SHA256 比对：完全一致（含反向核查：全局无多余文件）。"双处须同步"约定当前
处于遵守状态。无建议，仅记录基线。

## A-02 `.agents` 版 review-code 停留在 ARMCC V5 维度（Keil 退役后未跟进）⚠️

- **位置**：`%USERPROFILE%\.agents\skills\review-code\SKILL.md`（vs `.zcode`/`.claude`
  两根的版本，后两者已同步为 GCC 版）。
- **事实**：三根比对，review-code 在 .agents 与 .zcode/.claude 间有 23 行 diff：
  .zcode 版第 3 审查维度已是 `gcc_compat`（-Wall -Wextra、-fno-common、隐式声明等），
  .agents 版仍是 `armcc_v5`（NO stdbool.h、C89 声明序、ASCII 注释等）。
- **判断**：§2 #7 已定案 Keil 退役、GCC 为唯一 AI 链路（1e34524、6405b39）。.agents 版
  对现役工程已过时；若刻意保留供旧 ARMCC 工程复用，缺一行"已退役/仅存档"的口径说明。
- **建议**：主控择一——① 将 .agents 版同步为 GCC 版（若三根本应同源）；
  ② 或在 .agents 版头部标注"ARMCC V5 存档版，AI 链路请用 GCC 版"，避免异构智能体
  误用旧维度审查新工程。

## A-03 review-hardfault / feedback-log 在 .agents 根有轻微漂移 ⚠️

- `review-hardfault`：.agents 与 .zcode/.claude 仅 description 行措辞差异（2 行 diff）。
- `feedback-log`：4 行 diff，含"knowledge base 双工具链共用"表述的差异
  （.zcode 版提到 keil-error-db.json 保留 ARMCC 历史条目 + GCC 新条目，.agents 版表述不同）。
- **建议**：低优先级；下次维护全局 skill 时顺手对齐三根措辞，避免"哪个是 canonical"
  的疑问（同 A-02 一起处理即可）。

## A-04 button-toggle 工程缺 `.workbench/feedback` 目录（F-001 现实佐证）

- **位置**：`<工作区根>\stm32f103-button-toggle\.workbench\`（只读核查，2026-08-30）。
- **事实**：无 `feedback/` 子目录；adc-oled 有。=> 该工程历次 verify（非 gate-run）
  的反馈落账全部命中原 F-001 死锁并被 verify 静默吞掉，校准数据从工程建成起为空。
- **判断**：toolkit 侧 F-001 修复（handoff 分支 4866fb5）后，下次在该工程跑 verify
  会自动创建目录并开始落账，无需人工干预。
- **建议**：无需动作；若主控希望补齐历史空窗说明，可在工程日志记一笔"feedback
  落账自 F-001 修复（2026-08-30）起才实际生效"。

## A-05 四个全局 skill 职责边界：清晰，无重叠/缺口 ✅

- review-code=编译前五维对抗审查；fresh-checker=完工后无上下文二方审计；
  review-hardfault=HardFault 根因对立审查；feedback-log=诊断→验证事件落账与校准。
  四者分别卡在生成→构建→故障→落账四个不同环节，触发词不冲突，无职责空洞。
- 唯一交叉点：review-hardfault 与 feedback-log 都消费 fault_type —— 语义一致
  （hardfault.py 的 fault_type 字段），无冲突。无建议。

## 附：两工程 .workbench 契约兼容性核查 ✅

- 两工程 config.json 顶层键结构一致（builder/capture/gcc/physical_gate/
  toolkit_min_version/verify），均 builder=gcc、均有 expectations.json 与 releases/
  （adc-oled v1.1.0、button-toggle v1.0.0），state.json last_build 键结构一致
  （gcc_build 写入格式统一）。
- capture.backend 均 rtt，端口错开（adc-oled 19021 / button-toggle 19022），无冲突。
- toolkit_min_version=0.1，当前 toolkit VERSION 满足（version_ok 通过）。
- 结论：契约格式兼容，无分叉。无建议。
