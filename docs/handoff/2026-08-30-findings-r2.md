# 2026-08-30 代管 R2 发现报告（第二轮异构审查）

> 代管者：第二轮外部异构审查智能体｜分支 `handoff/zcode-r2-20260830`｜基线 89 tests 绿起步。
> 考卷 = 简报专项题 A/B/C/D（G1 时序窗口 + state.json 竞态 → 写回型工具全仓扫 →
> 零覆盖补测 → 文档对齐/VERSION/expectations_lint）。通用四级巡检结论并入各节。
>
> **⚠️ 2026-08-31 换回对账增补（主控，全文见 `2026-08-31-r2-reconcile-notes.md`）**：
> 本报告产自起点偏移的分支树（merge-base 实为 9997ac2，而非上岗戳 aae9084 宣告的基线
> 2bd0c17 所在链）。§〇"主控文档未落盘"的推断系误判——专项题/#10#11/106 基线均已在
> master 落盘，只是本分支看不到。本报告三项发现重排为 **F-018/F-019/F-020**
> （原编号与 master 1819e18 已编目的另三个问题冲突）。正文保留作者原编号为历史记录。

## 〇、开局矛盾记录（简报 vs 磁盘，§5 第 6 条处置）

| 简报说 | 磁盘实际 | 处置 |
|---|---|---|
| §2 勿重做清单 11 行（#10/#11 防重报条目） | 9 行，无 #10/#11 | 本轮 D 项 docs commit 补齐 |
| §3 是专项题 A/B/C/D | §3 为通用四级巡检，无专项题 | 以简报为准执行，§3 原文未动 |
| onboarding 应见 106 tests OK | 89 tests OK（与 Day 3 日志一致） | 记录差异；R2 收官 155 |
| "F-017 一测就中的教训" | `F-015/F-016/F-017` 全仓无任何引用，findings 无 R2 编目 | 推断主控预编号未落盘；本报告按 A→F-015/F-016、B→F-017 落位 |

## 一、代码级（A 专项 + B 全仓扫）

### F-018（Medium，已修；对账重排，原编号 F-015）发布记录不绑定判绿契约 — G1 时序窗口的取证缺口

- 位置：`scripts/release.py:158`（build_record 无契约字段）、`scripts/verify.py:929`
  （result 无哈希）、`scripts/release_audit.py`（R1~R6 只查记录内部自洽，无外部锚）。
- 窗口分析：G0 clean → G1 重跑（≤600s）→ finalize。`finalize` 用
  `git status --porcelain -uall` 拦 HEAD 变动与 foreign 改动（含未跟踪），两工程
  `config.json`/`expectations.json` 均被 git 跟踪（`git ls-files` 证实）→ **G0 之后的
  持久改动会被回滚，常规窗口已关**。
- 残余缺口（复现法）：G1 窗口内改 `expectations.json` 使判绿、finalize 前**还原**
  → 工作树 clean，记录+tag 落地，事后 release_audit R1~R6 全过——results 实际按
  篡改版清单判出。即便无对手，记录也无法回答"results 对应哪版清单"。
  测试固化：`tests/test_release_audit.py::test_tampered_contract_hash_fails`、
  `test_moved_record_contract_mismatch_fails`。
- 定性：Medium（正常流程有双重保护；缺口是对抗场景+事后取证）。与 M2 哲学对齐：
  hex 有锚（"烧的字节"），判绿无锚（"拿什么判的绿"）——不对称。
- 修复（34a96e5，9 例回归）：verify 落 `contract_hashes`（字节级 sha256）→
  release 抄进记录 `contracts` → release_audit 新增 **R7**（对照
  `git show <git_head>:` 重算比对，错位=fail；R7 之前旧记录=warn 不阻断）。

### F-019（Medium，已修；对账重排，原编号 F-016）state.json 非原子写 + 读改写损坏清空

- 位置：`scripts/wb_runtime.py:150`（旧 save_json_file 直接 truncate-write）、
  `scripts/wb_runtime.py:180`（旧 update_state_entry 读改写），openocd_runtime.py /
  serial_runtime.py 同构拷贝，`scripts/serial_mux.py:215,356,423`（手动读改写）。
- 复现法：① 并发：写者 truncate 后，读者（verify `--no-build`、release
  build_record）读到半截 JSON——下游均 fail-closed（F-007 报"无产物"、M2 拒发布），
  危害有限但正是②的源头；② state.json 损坏（撕裂/手工改坏）后任意一次
  `update_state_entry` → 文件被重写成只剩本条目，其他条目无声蒸发。
- 修复（351e021，17 例回归）：`save_json_file` 改 `.tmp + os.replace` 原子写；
  读改写改走 `load_workspace_state_for_update`——损坏**隔离 .corrupt 保留现场**后
  按新内容重建（F-014 同款语义；state.json 属可再生缓存，不拒绝写回）。
- 遗留（记录不修）：并发"lost update"（两个构建进程互覆 last_build）未加锁——
  单操作者工作流 + 硬件串行纪律下属可接受语义，加锁成本大于收益。

### F-020（High，已修；对账重排，原编号 F-017）写回型工具"损坏→清空"家族（B 专项旗舰）

- 位置与根因：`load_json_file` "损坏返回 {}" 本是**只读消费方**的容错（F-007），
  却被所有**读改写**路径当基础：当空读入 → 合并 → 覆写。
  1. `scripts/gcc_build.py:237`（旧直接写回）：config.json 损坏 + 任意一次成功构建
     → config.json 只剩 `{"gcc": ...}`，verify/openocd/capture/physical_gate/
     **toolkit_min_version** 段无声蒸发（版本门禁随之失效）。
  2. `wb_runtime/openocd_runtime/serial_runtime` 的 `save_project_config`：同款 wipe。
  3. `scripts/error_db_grow.py:149`（旧 _cache_entry）：session_fix_cache.json 损坏
     → 待人审条目被静默清空。
  4. `scripts/error_db_grow.py:259`（旧 grow）：知识库损坏裸 traceback（数据安全但
     违反 F-014 确立的容错纪律）。
- 复现法（一测就中，F-017 教训的注脚——修复前默认写回路径**零测试**）：
  ```
  echo "{ broken" > <工程>/.workbench/config.json
  python scripts/gcc_build.py build   # 任意一次成功构建
  → config.json 被重写为只剩 gcc 段
  ```
  测试固化：`tests/test_writeback_guards.py::GccBuildWritebackTests`。
- 修复（351e021）：config.json 是验证契约 → 损坏**拒绝写回**（gcc_build 侧
  `result["config_writeback"]=skipped_corrupt` 留痕，runtime 侧返回 None 不写）；
  session_fix_cache 损坏隔离 .corrupt 后重建；知识库损坏明确报错拒绝写入。
  三份 runtime 拷贝同修（"按脚本复制不做共享模块"哲学下的同构契约）。
- B 扫描其余结论（无发现）：`cube_to_keil.py:276` 原地改写源码**有 .bak 备份**；
  `svd_to_json` 无 `--out` 只写 stdout；`gen_periph.py:658` 默认写
  `<工程>/modules/<外设>/` 属文档生成的设计意图且有 `--out` 覆盖；feedback_db 两个
  JSON 的写路径 R1 已修（.corrupt 容错），本轮未再动。

## 二、机制级（A1 结论 + 旁路复查）

- G0~G3 旁路面复查：`builder` 篡改为 keil → `--rebuild` 直接报错拒绝；无
  expectations.json → G1 fail "缺 results 数组"；`--allow-xfail` 仅豁免 XFAIL 且
  ID 留痕入档（R1 结论维持）。
- G1 时序窗口结论见 F-015：常规窗口由 clean-tree + finalize foreign 检查关闭，
  对抗窗口由 R7 契约锚关闭。

## 三、文档级（D 项）

- 过期计数：HANDOFF-AGENT.md §1 "28 个脚本/**47 测试**"、§4 "**当前基线 47 绿**"、
  AGENTS.md "47 测试" → a1ae583 改为动态表述（"以 unittest 实际输出为准"），
  避免再次过期。
- §2 缺 #10/#11 防重报条目（简报预告但磁盘缺失）→ a1ae583 补齐：#10 = F-001~F-005
  首轮修复组（4866fb5/1baeed2/11eb319），#11 = F-014 + Low 组 + release_audit
  （afc7265/fa5efb4/eb1e4c9）。
- 新建 `CHANGELOG.md`（0.1.x R1 全账 + 0.2 R2 全账）；`VERSION` 0.1→0.2。
  兼容性：两工程 `toolkit_min_version` 均为 "0.1"，`version_ok` 语义下 0.2 ≥ 0.1 通过。

## 四、系统级（只读快查）

- fresh-checker 双处同步义务复核：toolkit `skills/fresh-checker/SKILL.md` vs
  `C:\Users\<用户名>\.claude\skills\...` vs `C:\Users\<用户名>\.agents\skills\...`，
  `fc /b` **零差异**——无漂移。
- 四全局 skill 职责重叠深审未做（R1 已两轮结论，本轮简报未列，留 R3）。

## 五、C 项补测清单（零覆盖模块，纯 mock/临时文件）

| 新测试文件 | 例数 | 覆盖对象 |
|---|---|---|
| tests/test_writeback_guards.py | 17 | 三份 runtime 写回守卫 + gcc_build + error_db_grow |
| tests/test_zero_coverage_pure.py | 21 | phase_minus_one 四查/裁定、rm_lookup 四路检索、token_stats、svd_to_json 解析基元 |
| tests/test_expectations_lint.py | 19 | E1~E9 全规则 + 两现役工程真档冒烟 CLEAN |
| tests/test_verify_expectations.py（增） | 4 | contract_hashes 字节敏感/legacy/空仓 |
| tests/test_release.py（增）+ test_release_audit.py（增/改） | 5 | build_record 契约传递 + R7 四态 |

套件：**89 → 155 全绿**。

## 六、遗留与提示

1. **release_audit 对 R7 前旧记录转 WARNED 属预期**：adc-oled v1.1.0 /
   button-toggle v1.0.0 的既有记录缺 `contracts` 键，R7 将给 warn（诚实反映
   "旧记录不可契约锚定"）；下次真实发布后新记录自愈。插板验证清单第 5 步
   （release_audit 冒烟）若见 WARNED 以此为准。
2. F-015 端到端（verify→release→audit 全链）含打 tag，代管期不可演练；
   离线层面已由 155 例中的 R7/contract 场景覆盖，下次真实发布自然终判。
   除此之外**本轮无新增待真机终判项**（F-003/F-005 沿用 R1 结论）。
3. expectations_lint 建议纳入换回协议第 3 步（`unittest 全绿 + 抽查编译`旁加
   `python scripts/expectations_lint.py --project <工程>`，秒级只读）。
4. 本轮 5 commit：34a96e5（F-015）/ 351e021（F-016/017）/ cc54e45（C）/
   10507bd（lint）/ 本 docs commit（D 文档+本报告+日志）。
   插曲：guard 曾对 F-016/017 commit 的**注释措辞**误报 openocd_call block
   （注释里 "openocd" 与 "verify" 同行共现）——分支未推送, 以历史重放消解,
   守卫脚本与 allowlist 未动（L1 禁线）。修复类均带回归；未触 master、两工程、
   硬件、machine.json、hooks/、handoff_guard.py。
