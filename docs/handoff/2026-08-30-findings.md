# 代管期发现报告 — 2026-08-30（Z code）

> 代管者：Z code（外部异构审查智能体）｜分支 `handoff/zcode-20260830`｜上岗自检 47 tests OK。
> 巡检深度说明：核心门禁链（verify.py / gcc_build.py / release.py / feedback_db.py / wb_runtime /
> wb_common / handoff_guard）逐行细读；hardfault.py（被 verify 子进程调用，属链上脚本）逐行细读；
> 其余 scripts/（gen_periph、svd_to_json、rm_lookup、token_stats、phase_minus_one、cube_to_keil、
> openocd_* 家族、serial_* 家族）做模式级批量扫描（subprocess 超时纪律 / 裸 except / open 无
> encoding / 静默 pass）+ 疑点上下文抽查，未逐行。
> 复现均在沙盒临时目录完成，未触碰两工程、未运行任何硬件段。

## 一、Critical（已修，带回归测试）

### F-001 feedback_db 首次落账死锁 → verify 双重静默丢反馈

- **位置**：`scripts/feedback_db.py:25-44`（`_project_feedback_dir` / `_feedback_dir`）+
  `scripts/verify.py:1254-1259`（静默吞）。
- **现象**：工程根存在但 `.workbench/feedback` 目录尚不存在（新工程、或建成以来从未成功落账的
  工程）时，`_project_feedback_dir()` 因两个候选目录都 `isdir()==False` 返回 None，
  `_feedback_dir()` 打印 **误导性错误**"Error: 未找到工程根"并 `sys.exit(1)`。
  而 `log_event` 本应通过 `os.makedirs(events_dir)` 自动建目录——但永远执行不到。
  上游 verify.py 的调用处 `except Exception: pass` 把退出码 1 完全吞掉，主流程无任何痕迹。
- **复现**（沙盒临时目录，2026-08-30 实测）：
  1. `tmp/.workbench/config.json` 写 `{}`；cwd=tmp；
  2. `python <工作区根>\embedded-toolkit\scripts\feedback_db.py --log "{\"pipeline\":\"build_fix\",\"outcome\":\"fixed\"}"`
  3. 实际：returncode=1，stderr=`Error: 未找到工程根 (...)`，feedback 目录未被创建；
     期望：returncode=0，`.workbench/feedback/` 自动创建，事件落账。
  4. 对照组 `--stats build_fix` 同样 exit 1（读路径也被同一误报挡死）。
- **现实影响面**：`<工作区根>\stm32f103-button-toggle`（现役 v1.0.0）**没有 feedback 目录**
  （2026-08-30 只读核查）——该工程历次 verify 的反馈事件全部静默丢失，无人察觉。
  §3 点名的"feedback_db 落账在哪些失败模式会静默跳过（历史上发生过）"，这就是现行犯。
- **严重度**：Critical（静默丢数据）。
- **修复**：`_project_feedback_dir()` 在工程根已找到但无既有 feedback 目录时，返回首选
  `.workbench/feedback` 让调用方 makedirs 创建（`.embeddedskills/feedback` 存在时优先级
  不变，迁移兼容工程零影响）。回归测试 `tests/test_feedback_db.py`（4 例：首次落账建目录、
  空工程 stats 返回零而非 exit 1、.embeddedskills 既有目录仍被优先、真无工程根时错误语义
  正确）。commit `fix(handoff): F-001 …`。

## 二、Medium（Day 2 已全部修复）

> 状态回填（2026-08-30 Day 2）：用户认可"失败路径必须留痕"改进方向后，按 §5 纪律
> 全部带回归测试修复。四个修复合两组 commit：verify.py 三连 `1baeed2`（F-002/003/004，
> 带 tests/test_verify_failure_paths.py 19 例）、hardfault `11eb319`（F-005，带
> tests/test_hardfault_map.py 4 例）。F-003/F-005 涉真机归因链，标注**待真机终判**。

### F-002 verify.py 对损坏 config.json 裸 traceback（M1 同类盲区）【已修 Day 2】

- **位置**：`scripts/verify.py:237-244`（`load_config`，`json.load` 无容错）。
- **复现**：临时工程 `.workbench/config.json` 写入 `{broken`，cwd 后跑
  `python scripts/verify.py --json` → returncode=1 + 裸 `json.decoder.JSONDecodeError` traceback，
  而非 M1 后 expectations.json 那样的友好错误行。
- **影响**：工程配置损坏（手改坏/合并冲突残留）时用户拿到的是 traceback 而非可行动错误；
  与已修的 expectations 四连坑同类，但 config.json 未盖到。
- **建议**：`load_config` 捕获 `JSONDecodeError/UnicodeDecodeError`，转译为带文件路径的
  友好错误并 exit 1。修复属 verify.py 主流程（L3 需 tests/ 伴随），留主控拍板。

### F-003 verify.py semihosting 采集超时：丢弃已收输出 + capture 谎报 ok【已修 Day 2，待真机终判】

- **位置**：`scripts/verify.py:1079-1082`（`except subprocess.TimeoutExpired` 分支）。
- **现象**：OpenOCD 卡死触发 communicate 超时后，`proc.kill()` 再 `communicate()` 能收回
  **部分输出**，但代码强制 `captured_lines = []` 并丢弃 stdout/stderr；随后 capture 步骤
  以 `status=ok, lines=0` 入账，验证环节把"采集工具超时"伪装成"程序无输出"归因。
- **复现**：需真机（OpenOCD 卡死场景）；机制可用 mock Popen 抛 TimeoutExpired 的单测覆盖
  （assert captured_text 保留部分输出、capture.status != "ok"）。
- **建议**：超时分支解析已收输出、capture.status 标 `timeout`，走 capture_failed 早退。
  注意这只影响 OpenOCD 卡死路径（正常路径 sleep+halt+shutdown 必然退出），不改变正常
  行为。**待真机终判**（修复前后真机各跑一次确认归因链）。

### F-004 verify.py feedback 落账结果无痕（审计盲区）【已修 Day 2】

- **位置**：`scripts/verify.py:1240-1259`。
- **现象**：三条静默路径，主流程 JSON 均无标注，消费方（Claude/脚本）无法区分
  "落账成功/门禁跳过/落账失败"：
  1. `--gate-run` 跳过（by design，但 result 无 `feedback.skipped` 类标注）；
  2. feedback_db.py 崩溃（如 F-001 的 exit 1）被 `except Exception: pass` 吞；
  3. 子进程 10s 超时同吞。
- **建议**：result 增加 `feedback: {logged: bool, skipped?: true, error?: str}` 一类字段
  （不改变退出码与主流程），F-001 修复后该字段可用于持续监控落账健康度。
  属 verify.py 改动（L3），留主控拍板。

### F-005 hardfault.py WORKSPACE 语义错误 + 默认 map 失效（blink 残留）【已修 Day 2（map 自动发现），待真机终判】

- **位置**：`scripts/hardfault.py:30-31`（`WORKSPACE = os.path.dirname(SCRIPT_DIR)` 是
  **toolkit 仓根**而非工程根，变量名/注释双误导）、`:399`（默认 map =
  `<仓根>/lst/blink.map`，blink 退役后该路径不存在）、`:366`（帮助文本同残留学）。
- **现象**：verify.py Step 4b 调 `hardfault.py --json` 时不传 `--map` → `parse_map_symbols`
  拿到不存在的路径返回 `[]` → 符号解析永远为空（诊断降级为"no matching symbol"，
  fault_type 判定不受影响——不致命，故 Medium）。
- **复现**：静态即可判定（仓根无 lst/ 目录）；真机症状=诊断报告 symbols_total: 0。
- **建议**：map 默认改为从 cwd 向上 `find_project_root` 后在工程内查 `lst/*.map`
  （或按 Makefile TARGET 匹配）；WORKSPACE 更名/revert 为"工具仓根"正名。
  修复不触碰 OpenOCD 交互行，可单测路径逻辑；但 hardfault 属真机域，
  端到端效果**待真机终判**。

## 三、Low（记录，不修）

- **F-006** `scripts/gcc_build.py:250`：`import time` 在 `if __name__ == "__main__"` 块内，
  而 `main()`（:146,:204）引用 `time`——任何 `import gcc_build` 再调 `main()` 的未来调用方
  会 NameError。当前无 import 方调用（verify.py 走 subprocess，已核实），无现行触发路径。
- **F-007** `scripts/verify.py:962,964`：`--no-build` 路径 hex 兜底默认 `"obj/blink.hex"`
  是 blink 退役残留；state.json 缺 hex 时报 `hex file not found: obj/blink.hex` 误导排查。
- **F-008** `scripts/verify.py:900-903,933`：`step_analyze` 在 build 成功路径被调用两次
  （gcc 后端为纯包装无害；keil legacy 路径会双跑 keil_analyze，浪费+潜在双写状态）。
- **F-009** `scripts/release.py:213-214`：finalize 的 foreign 检查用
  `endswith(rec_rel)` 判定"记录文件自身"，工作树中恰有同后缀路径（如
  `sub/.workbench/releases/vX.json`）时会被漏判为 foreign。攻击向量弱，记录备查。
- **F-010** `scripts/verify.py:276`：`run_cmd` 成功判据
  `returncode == 0 or 'verified' in stdout.lower()`——后半是对克隆适配器偶发非零退出的
  内容兜底（同 swd_probe 哲学），但子串判据偏宽；烧录判定属真机域，记录不改。
- **F-011** `scripts/verify.py:39`、`scripts/release.py:252`、`scripts/hardfault.py:33`：
  `load_machine()["openocd_exe"]` 缺键即裸 KeyError（含 --help 前崩溃）。
  machine.json 本机健在且只读，纯防御性问题。
- **F-012** `scripts/verify.py:518-529`：`extract_semihosting_text` 死代码（无调用点，
  2026-08-30 grep 核实；docstring 自述"需要重新考虑"）。
- **F-013** `scripts/hardfault.py:432`：JSON `status` 恒为 `"hardfault_detected"`，
  即使 `fault_type == "no_fault"`；verify.py 消费端靠 fault_type 区分故未出错，
  但契约语义含糊，第三方消费者易误读。

## 四、机制级结论（考卷答案，非缺陷）

- **M-1 `--gate-run`/`--rebuild` 无旁路**：`--gate-run` 仅跳过 feedback 落账
  （verify.py:1242），不影响判定；`--rebuild` 走 gcc_build clean+rebuild。release.py G1 以
  `cwd=ws` 子进程重跑 verify，G0 要求工作树 clean，finalize 打 tag 前复核 HEAD 未变且
  无"记录文件之外"的改动（-uall 展开）——G1 重跑期间外部篡改窗口被 finalize 封住。
- **M-2 G0-G3 不可绕**：XPASS 在 `evaluate_expectations` verdict 层即判红
  （verify.py:650），G1 顶层 status 检查接住；G2 xfail 须显式 `--allow-xfail` 且 ID 落档
  （release.py:149-153）；hex 哈希缺失强制中止（release.py:263-266，审计 M2 仍在位）。
- **M-3 releases/*.json 伪造面**：发布记录与 annotated tag 均为本地 git 操作，无签名；
  记录文件可手改、tag 可 `git tag` 手打绕过 release.py，且仓库内**无事后审计工具**复核
  （如：按 tag 重算 verify 结果/比对 hex sha256 与 tag commit 字节）。门禁防"糊涂"
  不防"蓄意"（guard 同款威胁模型）。建议见 advice 单 A-02。
- **M-4 §2 勿重做清单与现状无矛盾**：8 个证据 commit（e96718b/83445ed/9594dbb/ad9c625/
  5ffeb3a/d2c572a/1e34524/6405b39）全部在案且主题对得上；#8 UART 搁置（无相关改动）与
  #9 CMSIS 瘦身搁置（Drivers 未瘦身）状态属实。

## 五、文档级结论

- **D-1** README.md / AGENTS.md / HANDOFF-AGENT.md 抽查（期望清单规则、--rebuild/--gate-run
  语义、Keil 退役口径、.workbench 版控策略、机器路径唯一来源）与代码现状一致，无漂移。
- **D-2** blink 失效引用残留三处：hardfault.py:8/366/399（并入 F-005）、verify.py:962,964
  （F-007）；verify.py:304-305 的 `blink.uvprojx` 默认值属 keil 退役路径，builder 显式
  选 keil 才可达，无实际影响。verify.py:488 "no steady blink pattern" 为物理门控措辞，
  描述具体场景，不算失效。

## 六、系统级

仅建议单（§3 纪律），见 [2026-08-30-advice.md](2026-08-30-advice.md)：A-01 fresh-checker
双份一致（无漂移，正面结论）、A-02 .agents 版 review-code 停留在 ARMCC V5 维度、
A-03 review-hardfault/feedback-log 轻微漂移、A-04 button-toggle 无 feedback 目录
（F-001 现实佐证，修复后自动解决）、A-05 四 skill 职责边界清晰无重叠。

## 七、Day 3 增补（用户授权"失败路径留痕"方向后处置）

### F-014 feedback_db 校准库损坏 → 全部后续落账崩死（Medium，已修 afc7265）

- **位置**：`scripts/feedback_db.py:76-94`（Day 3 修复前编号 76-94/89-94）。
- **现象**：`load_feedback_db`/`load_calibration` 为裸 `json.load` — feedback_db.json 或
  calibration.json 一旦损坏（手改坏/半截写入），所有后续 `--log` 全部 traceback 崩死，
  校准数据静默断流（F-004 修复前 verify 侧同样无痕）。另 `log_event` 为"先写事件文件、
  再更新索引"两步写，索引写失败时事件孤悬（文件在但 `find_similar` 不可见）——
  该顺序实为较优（数据可手工恢复），Day 3 以回归测试固化该语义，不改变。
- **复现**：工程 feedback 目录放损坏 feedback_db.json → 跑 `--log` → 修复前裸 traceback；
  修复后原文件移 `.corrupt` 保留现场 + 空库重建 + stderr 警告，落账继续。
- **修复**：`_load_json_or_rebuild` 公共容错（损坏备份重建）；顺带补全空库模板缺
  verify/fresh_check 计数键（与 valid_pipelines 对齐，否则这两类事件只涨总数不涨分类）。
  带 4 例回归（tests/test_feedback_db.py 追加）。

### Low 项处置表（F-006~F-013）

| # | 处置 | 理由 |
|---|---|---|
| F-006 | **修**（fa5efb4） | import time 上移模块顶部，一行消除 import 埋雷；带 hasattr 回归 |
| F-007 | **修**（fa5efb4） | --no-build 无产物改明确报错 + state.json 读容错（损坏不再裸 traceback）；blink 残留 fallback 移除；带 step_flash 回归 |
| F-008 | **修**（fa5efb4） | analyze 复用 build 循环内结果，keil 后端不再双跑 keil_analyze（逻辑等价，循环 break 路径必已赋值，None 防御保底） |
| F-012 | **修**（fa5efb4） | 死代码 extract_semihosting_text 删除（无调用点，Day 1 已核实） |
| F-009 | 记录不修 | finalize foreign 检查 endswith 弱匹配，攻击向量弱；收紧需重设计匹配规则，收益<改动风险 |
| F-010 | 记录不修 | 'verified' 子串判据是对克隆适配器偶发非零退出的内容兜底（有意设计）；收紧需真机验证输出形态 |
| F-011 | 记录不修 | machine.json 本机健在且只读；防御性 .get() 会引入"空串静默传递"新风险（openocd_exe="" 时 subprocess 报错更难懂），防御收益<新增静默 |
| F-013 | 记录不修 | status 恒 hardfault_detected 语义含糊，但仓内唯一消费者 verify.py 走 fault_type 字段；改 status 需为一行造重型 mock 测试，性价比低，留文档层说明 |

### 六大脚本补扫结论（gen_periph / openocd_gdb / svd_to_json / serial_mux /
openocd_run / openocd_telnet，共约 3800 行）

- 深度模式扫描（shell=True / os.system / 可变默认参数 / 宽 except / eval-exec /
  JSON 无容错 / communicate 无超时）+ 命中处人工抽查：**无新发现（Low 以下无项）**。
- serial_mux 4 处宽 except 均带正确副作用（stop_event 优雅停机 / failed 留痕列表），
  非吞错；openocd_gdb/telnet 的 Popen 无 timeout 属交互式长会话设计（wait_server_ready
  轮询 + Ctrl+C 退出契约）；gen_periph/svd_to_json 读仓库内受控 ref 文件，
  独立 CLI 工具 traceback 可接受。
- openocd_run.py 零命中，最干净。

### 新工具：release_audit.py 发布记录事后审计（M-3 处置，commit eb1e4c9）

- **背景**：M-3 结论——releases/*.json 与 annotated tag 均为本地 git 操作，无签名，
  事后可被手工篡改且无复核工具。用户授权补上这块（新功能，超出 fix 范围故单列）。
- **能力**：R1 记录可解析 / R2 tag 指向 commit == 记录 git_head / R3 hex 字节重算
  sha256 比对（文件缺失降级警告——build/ 可清理）/ R4 results 无 FAIL/XPASS 且
  xfail_waived 与 XFAIL 集合相等 / R5 结构完整 / R6 记录已入库（警告）。
  只读、不触硬件、退出码 0/1/2。
- **验证**：11 例篡改场景回归（test_release_audit.py：字节篡改/HEAD 不符/丢 tag/
  FAIL 条目/XPASS 条目/豁免不自洽/记录损坏等）；真实冒烟——
  `python scripts/release_audit.py --project <工作区根>\stm32f103-adc-oled --tag v1.1.0`
  → **CLEAN**（R1~R6 全过，hex 哈希重算一致）。
- **建议**：主控可将其纳入换回协议第 2 步（guard 之后），对工程既有 tag 各跑一次。

### 插板验证清单（换回后用户执行，「待真机终判」项的终判步骤）

1. `python scripts/verify.py --json`（adc-oled，接板）→ 全绿，且 JSON 含
   `feedback.logged: true` + event_id（F-001/F-004 终判）。
2. 对照 `.workbench/feedback/feedback_db.json`：total_events 已 +1，button-toggle
   跑一次同样落账（该工程 feedback 目录将首次自动创建）。
3. （可选，F-003 终判）确认正常路径无回归：capture.status==ok、正文行数与升级前相当。
4. （可选，F-005 终判）`python scripts/hardfault.py`（在工程目录内）→ 报告
   `symbols_total` 非 0（自动发现 lst/*.map 生效）。
5. `python scripts/release_audit.py --project <两工程> --all` → 双 CLEAN。
