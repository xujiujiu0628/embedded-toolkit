# 安全漏洞报告（Security Policy）

## 报告渠道：请先私下报告，不要开公开 Issue

本工具链会以调试器接管、烧录、向 SRAM 读写的方式直接操作你的开发板与探针，
漏洞可能同时危及**主机与硬件**。因此发现安全问题时请私下报告：

1. **首选**：GitHub Security Advisory ——
   [Report a vulnerability](https://github.com/xujiujiu0628/embedded-toolkit/security/advisories/new)
   （私有讨论区，修复公开前其他人在仓库中不可见）
2. **备选**：邮件 `xujiujiu0628@users.noreply.github.com`
   （GitHub 匿名转发；可替换为维护者公开邮箱）

## 请提供

- 受影响脚本与版本（`git rev-parse HEAD` 或 `VERSION` 文件内容）
- 最小复现步骤或 POC 脚本
- 平台信息（Windows/Linux、Python 版本、工具链版本）
- 潜在影响评估（如：路径注入、任意文件写、命令注入面）

## 响应时间预期

| 阶段 | 时限 |
|---|---|
| 确认收到报告 | 7 天内 |
| 初步评估与定级 | 14 天内 |
| 修复与致谢（经你同意） | 视严重度另议，目标 ≤90 天 |

本项目由单人以业余时间维护，时限尽力而为；严重且可被利用的问题会优先处理。

## 披露政策

在修复发布（或确认无法复现/不受影响）之前，请勿公开漏洞细节。
我们支持协调披露：修复上线后欢迎发布 advisory 与致谢；若 90 天内未获回音，
你有权自行公开。

## 范围说明

- **在范围内**：本仓库 `scripts/`、`hooks/`、门禁与审计逻辑中的安全缺陷。
- **不在范围内**：你的固件代码本身、STM32 芯片漏洞、`machine.json` 等
  本机配置文件的内容（它们不应含任何凭证——含凭证属配置事故，请按上面对照处理）。

## 硬件风险免责

工具按"如实判定"设计，但 SWD 擦写与烧录客观上可能改变甚至损毁目标板。
请在可牺牲的开发板上使用；夹具、目标板损坏风险自担。
